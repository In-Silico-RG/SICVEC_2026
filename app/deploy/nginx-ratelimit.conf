# Loaded in the http{} context via /etc/nginx/conf.d/.
limit_req_zone $binary_remote_addr zone=sicvec_forms:10m rate=6r/m;
limit_req_zone $binary_remote_addr zone=sicvec_admin:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=sicvec_all:10m   rate=10r/s;
